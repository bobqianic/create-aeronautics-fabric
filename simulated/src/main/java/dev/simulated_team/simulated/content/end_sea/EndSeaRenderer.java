package dev.simulated_team.simulated.content.end_sea;

import net.minecraft.client.Camera;
import net.minecraft.client.renderer.GameRenderer;

/** End-sea rendering is disabled on Fabric until a compatible shader pipeline is available. */
public final class EndSeaRenderer {
    private EndSeaRenderer() {
    }

    public static void tick() {
    }

    public static void render(Camera camera, GameRenderer gameRenderer) {
    }
}
